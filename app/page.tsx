export default function Dashboard() {
  return (
    // if user is not verified redirect to /login
    // if user is verified redirect to /home or sth
    <div>There will be something here soon</div>
  );
}
